<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="saveDishFlavor" parameterType="DishFlavor">
        insert into dish_flavor (dish_id ,name, value)
        values
        <foreach collection="list" item="dishFlavor" separator=",">
                (#{dishFlavor.dishId}, #{dishFlavor.name}, #{dishFlavor.value})
        </foreach>
                </insert>
    <insert id="saveDish" parameterType="Dish"
            useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, create_time, update_time, create_user, update_user)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <select id="page" parameterType="com.sky.dto.DishPageQueryDTO"
            resultType="com.sky.vo.DishVO">
        select d.*, c.name as categoryName from dish d left outer join category c on d.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and d.name LIKE CONCAT('%', #{name}, '%')
            </if>
                 <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
        <if test="status != null">
                and status = #{status}
            </if>
        </where>
                                </select>
    <delete id="deleteDish">
    delete from dish where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <delete id="deleteDishFlavor">
        delete from dish_flavor where dish_id in
        <foreach collection="ids" item="dishId" separator="," open="(" close=")">
            #{dishId}
        </foreach>
    </delete>
    <!-- 1. 定义resultMap（需处理字段别名） -->
    <resultMap id="ResultDishVO" type="com.sky.vo.DishVO">
        <id property="id" column="dish_id"/>  <!-- 改用dish的id别名 -->
        <result property="name" column="dish_name"/>
        <result property="categoryId" column="category_id"/>
        <result property="price" column="price"/>
        <result property="image" column="image"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="updateTime" column="update_time"/>
        <result property="categoryName" column="categoryName"/>
        <!-- 映射口味信息，注意column对应查询语句的别名 -->
        <collection property="flavors" ofType="com.sky.entity.DishFlavor">
            <id property="id" column="flavor_id"/>  <!-- 口味表id的别名 -->
            <result property="name" column="flavor_name"/>
            <result property="value" column="flavor_value"/>
            <result property="dishId" column="dish_id"/>
        </collection>
    </resultMap>

    <!-- 2. 多表连接查询 -->
    <select id="getByIdWithFlavor" resultMap="ResultDishVO">
        select
        d.id as dish_id, d.name as dish_name, d.category_id, d.price, d.image,
        d.description, d.status, d.update_time, c.name as categoryName,
        df.id as flavor_id, df.name as flavor_name, df.value as flavor_value, df.dish_id
        from dish d
        left outer join category c on d.category_id = c.id
        left outer join dish_flavor df on d.id = df.dish_id  <!-- 关联口味表 -->
        where d.id = #{id}
    </select>
    <update id="update" parameterType="Dish">
        update dish
        <set>
        <if test="name != null">
            name = #{name},
        </if>
            <if test="categoryId != null">
            category_id = #{categoryId},
        </if>
            <if test="price != null">
            price = #{price},
        </if>
            <if test="image != null">
            image = #{image},
        </if>
            <if test="description != null">
            description = #{description},
        </if>
            <if test="status != null">
            status = #{status},
        </if>
            <if test="updateTime != null">
            update_time = #{updateTime},
        </if>
            <if test="updateUser != null">
            update_user = #{updateUser},
        </if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>